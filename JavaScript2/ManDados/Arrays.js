// Manipulando Arrays

// Criar Array com construtor
let myArray = newArray('a', 'b', 'c')
console.log(myArray)

// Manipulando Arrays

// Contar elementros de um array
console.log(["a", "b", "c"].lenght)

// Manipulando Arrays

// Transformar uma cadeia de caracteres eme lementos de um array

let word = "manipulação"
console.log(Array.from(word))